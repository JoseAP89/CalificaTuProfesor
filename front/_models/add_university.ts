export default class AddUniversity {
    name: string;
    img_file: string;

    constructor(name: string, img_file: string ) {
        this.name = name;
        this.img_file = img_file;
        
    }
}