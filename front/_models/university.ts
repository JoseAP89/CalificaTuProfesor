export default class University {
    university_id: number;
    name: string

    constructor(universiti_id: number, name: string) {
        this.university_id = universiti_id;
        this.name = name;
    }
}